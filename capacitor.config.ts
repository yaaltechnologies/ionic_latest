import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Vaanibam_mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
